import 'package:flutter_easy_auto/models/address.dart';

class Mechanics {
  String name;
  Address address;
  String specialty;
  String worksWith;

  Mechanics(this.name, this.address, this.specialty, this.worksWith);
}
